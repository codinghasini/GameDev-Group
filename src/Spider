class Spider {
  // Member Variables
  int x, y, w, speed;
  char type;
  PImage spider;
  //Constructor
  Spider() {
    x=int (random(width));
    y=-100;
    w=100;
    speed =int(random(1, 5));
   type='a';
  spider=loadImage("spider.png");

   
    }
 
